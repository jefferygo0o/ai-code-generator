{
  "name": "my-ai-code-generator",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "express": "^4.18.2",     // For local testing with Vercel if needed
    "openai": "^3.2.1",       // OpenAI API library
    "next": "latest",         // Next.js, since you're deploying on Vercel
    "react": "^18.2.0",       // React for the frontend
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "dev": "next dev",        // Runs the development server
    "build": "next build",    // Builds the app for production
    "start": "next start"     // Starts the production server
  }
}
